server:
  port: 8080
  servlet:
    context-path: /springboot

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/springboot"
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true